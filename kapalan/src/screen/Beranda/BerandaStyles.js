import { StyleSheet } from 'react-native';

const berandaStyles = StyleSheet.create({
    container: {
        backgroundColor: 'red',
        alignItems: 'center',
        justifyContent: 'center',
        flex: 1,
        paddingLeft: 30,
        paddingRight: 30,
    },

})

export default berandaStyles;