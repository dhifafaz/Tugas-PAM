import { StyleSheet } from 'react-native';

const pembatalanStyles = StyleSheet.create({
    itemContainer: {
        justifyContent: 'center',
        padding: 10,
        backgroundColor: 'transparant',
        alignContent: 'center',
        flex: 1,
    },

})

export default pembatalanStyles;